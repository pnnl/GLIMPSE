Flask
flask-cors
cim-graph
jsonschema
requests
wheel
setuptools

# Hosted-only
gunicorn        # WSGI server; no Socket.IO means no single-worker constraint
celery[redis]   # parse queue and worker fleet (see tasks.py)
redis           # job records, uploads, and results (see jobstore.py)
